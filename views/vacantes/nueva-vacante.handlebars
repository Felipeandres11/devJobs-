<main class="contenedor">
    <form action="/vacantes/nueva" method="POST" class="default-form">

        <h3>Información General</h3>

        <div class="campo">
            <label for="titulo">Titulo</label>
            <input type="text" name="titulo" id="titulo" placeholder="Ej: React developer" required>
        </div>

        <div class="campo">
            <label for="empresa">Empresa</label>
            <input type="text" name="empresa" id="empresa" placeholder="Empresa contrata" required>
        </div>

        <div class="campo">
            <label for="ubicacion">Ubicacion</label>
            <input type="text" name="ubicacion" id="ubicacion" placeholder="Ubicación" required>
        </div>

        <div class="campo">
            <label for="salario">Salario (USD) </label>
            <input type="text" name="salario" id="salario" placeholder="Ej: 500 USD" required>
        </div>

        <div class="campo">
            <label for="salario">Salario</label>
            <input type="text" name="salario" id="salario" placeholder="Salario" required>
        </div>

        <div class="campo">
            <label for="contrato">Contrato</label>
            <select name="contrato" id="">
                <option value="" disabled selected>-- Seleciona --</option>
                <option value="Freelance">Freelance</option>
                <option value="Tiempo completo">Tiempo completo</option>
                <option value="Medio tiempo">Medio tiempo</option>
                <option value="Por Proyecto">Por proyecto</option>
            </select>
        </div>
        
        <h3>Descripción del puesto</h3>

        <div class="campo descripcion">
            <label for="descripcion">Descripción</label>
            <input type="hidden" name="descripcion" id="x">
            <trix-editor input="x"></trix-editor>
        </div>

        <h3>Conocimientos</h3>
        <ul class="lista-conocimientos">
            {{#seleccionarSkills 0 }}
            {{/seleccionarSkills}}
        </ul>

        <div class="campo centrar-horizontal">
            <input type="submit" value="Publicar" class="btn btn-azul">
        </div>


    </form>
</main>